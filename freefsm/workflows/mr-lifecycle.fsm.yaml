# Flow:
#
#   create-mr → poll ←-----------+
#                |    \     \     |
#              check  fix   done  |
#              /   \    |         |
#           fix    poll push      |
#             |          |        |
#           push         +-------+
#             |
#             +-------------------+
#

version: 1
guide: |
  Monitor a MR/PR until it is merged or closed.
  Automatically fix CI failures and bot review blockers. Respond to `@bot` mentions
  for conversational replies or code changes.

  Supports GitLab (MR) and GitHub (PR). Platform is auto-detected from git remote URL:
  - GitLab: Uses GitLab REST API (requires PRIVATE-TOKEN)
  - GitHub: Prefers `gh` CLI, falls back to GitHub REST API

  ## Interaction model

  All user-initiated interaction goes through `@bot` mentions in inline review threads
  or issue-level (PR-level) comments. The `@bot` mention can trigger either:
  - A conversational reply (answer questions, explain code) — handled in-place during poll
  - A code modification (fix a bug, apply a suggestion) — exits poll to fix state

  All mr-lifecycle replies MUST be prefixed with `[from bot]` to distinguish agent-generated
  comments from human-authored ones.

  ## GitHub CLI tips (avoid common pitfalls)

  - **Prefer `gh pr` / `gh run` high-level commands** over raw `gh api` calls when possible.
  - **GraphQL queries**: Always inline owner/repo/PR values directly into the query string.
    Do NOT use `-f owner=... -f repo=...` variable binding — shell escaping breaks it.
    Example: `query { repository(owner: "myorg", name: "myrepo") { ... } }`
  - **jq and `@bot`**: When searching for `@bot` in API output, pipe through `grep '@bot'`
    rather than embedding in jq expressions to avoid shell escaping issues.
  - **Check runs**: Use `gh pr checks <N>` instead of raw `/commits/{sha}/check-runs` API.

initial: create-mr

states:
  create-mr:
    prompt: |
      First, detect the platform from git remote URL (gitlab.* → GitLab, github.com → GitHub).

      Verify authentication:
      - GitLab: Check for PRIVATE-TOKEN (environment variable or config file). If not found, prompt user to configure it.
      - GitHub: Check `gh auth status`. If not logged in, prompt user to run `gh auth login`.

      Once authenticated:
      - Run lint/format checks (refer to project CLAUDE.md for check commands). Fix any failures first.
      - Commit all local changes.
      - Check if the current branch already has an open MR/PR:
        - GitLab: Query via GitLab API
        - GitHub: Query via `gh pr list` or GitHub API
        If one exists, push the branch, record the URL and ID, and check if the description needs updating.
      - If none exists, create a new MR/PR:
        - Target branch: Use user-specified branch if provided, otherwise use the repo's default branch.
        - Compare against target branch and summarize changes as the description.
        - Use conventional commit format for the title.
        - GitLab: Create via GitLab API
        - GitHub: Create via `gh pr create` or GitHub API

      Record the MR/PR URL and ID — subsequent states will need them.
    transitions:
      MR ready: poll

  poll:
    prompt: |
      Run the bundled polling script as a **background bash job** so the conversation is not blocked.

      The script is at `poll_mr.py` in the same directory as this workflow YAML. Find it via:
      ```bash
      SCRIPT_DIR="$(dirname "$(readlink -f "$(freefsm current --run-id <run_id> -j | jq -r '.data.fsm_path')")")"
      ```
      Or locate it relative to this workflow file: `freefsm/workflows/poll_mr.py`

      ## Running the script

      ```bash
      python3 "$SCRIPT_DIR/poll_mr.py" <owner> <repo> <pr_number> --target <target_branch> --interval 20
      ```

      Run a single inline poll first to verify the script works (API auth, correct IDs),
      then start it as a background job.

      ## What the script monitors (every cycle)

      - **PR state**: merged or closed → exits
      - **CI status**: all checks finished (no PENDING) → exits
      - **Target branch**: new commits ahead of HEAD → exits
      - **`@bot` mentions**: scans inline threads and issue comments for unhandled mentions

      ## `@bot` dedup rules (implemented in the script)

      - **Inline threads**: a `@bot` mention is handled if a subsequent note starts with `[from bot]`
      - **Issue comments**: a `@bot` mention is handled if a `+1` reaction exists on the comment

      ## Exit RESULT lines

      The script prints one of these and exits:
      - `RESULT: MR merged` / `RESULT: MR closed` → transition to done
      - `RESULT: pipelines finished` → transition to check
      - `RESULT: bot mention code-change` → transition to fix (script prints @bot context)
      - `RESULT: bot mention conversational` → handle in-place, then restart the script

      ## Handling conversational `@bot` mentions

      When the script exits with `RESULT: bot mention conversational`, the script has printed
      the mention details (source, author, comment_id, body). You must:
      1. Read the printed context to understand what was asked
      2. Reply in the same thread / as a new comment with body starting with `[from bot]`
      3. For issue comments: add a `+1` reaction to the original comment as the dedup signal:
         `gh api repos/<owner>/<repo>/issues/comments/<id>/reactions -f content='+1'`
      4. For inline threads: the `[from bot]` reply note itself serves as the dedup signal
      5. Restart the polling script — conversational replies do NOT cause a state transition

      ## Handling code-change `@bot` mentions

      When the script exits with `RESULT: bot mention code-change`, the script has printed
      the mention details including file path and line for inline threads. Transition to fix.
      Do NOT reply yet — the push state will comment after the fix is applied.

      When running the script in the background, do NOT redirect stdout/stderr to a file —
      let output go to the terminal so the user can observe progress in real time.
      When the job completes, read its output from the task output and transition based on the RESULT line.
    transitions:
      pipelines finished: check
      fix requested: fix
      MR merged: done
      MR closed: done

  check:
    prompt: |
      All CI has completed or the target branch has been updated. Check for issues that need attention.

      ## Auto-fix round tracking

      Track `auto_fix_rounds` via a durable temp file `/tmp/mr_lifecycle_state.json`.
      - On first entry to check, read the file: `cat /tmp/mr_lifecycle_state.json 2>/dev/null`.
        If the file doesn't exist, initialize `auto_fix_rounds = 0` and write it:
        `echo '{"auto_fix_rounds": 0}' > /tmp/mr_lifecycle_state.json`
      - The max is 3. If `auto_fix_rounds >= 3`, skip bot review blockers entirely (see below).

      ## Check items

      **1. CI results**: Are there any failed jobs (GitLab pipeline / GitHub Actions checks)?
      CI failures are always actionable regardless of round count.

      **2. Bot inline review comments**: Fetch all unresolved review threads.
        - GitHub: Use a single-line GraphQL query with owner/repo/PR inlined directly (no `-f` variables).
          Filter for bot-authored threads: `comments.nodes[0].author.__typename == "Bot"`.
          Example:
          ```bash
          gh api graphql -f query='query { repository(owner: "OWNER", name: "REPO") { pullRequest(number: N) { reviewThreads(first: 100) { nodes { isResolved comments(first: 1) { nodes { author { login __typename } body } } } } } } }'
          ```
        - GitLab: Use MR discussions API, filter for `user.type == "Bot"`.

        **Severity parsing**: Bot review comments indicate severity via markers in the comment
        body. Look for patterns like `[BLOCKER]`, `[MAJOR]`, or HTML comments like
        `<!-- severity: blocker -->`, `<!-- severity: major -->`. Parse each unresolved
        bot-authored comment to determine its severity.

        **Severity filtering**:
        - **Blocker**: Include in fix list ONLY if `auto_fix_rounds < 3`.
        - **Major**: Do NOT include in fix list. Skip entirely — leave for the user to
          address manually or via `@bot`. Never auto-fix major severity comments.

        If `auto_fix_rounds >= 3`, skip ALL bot review comments (both blocker and major).
        Bot review blockers are no longer actionable after 3 rounds.

      **3. Rebase needed**: Is the current branch behind the target branch?
        Use `git rev-list --count HEAD..origin/<target>` to detect.
        Rebase is always actionable regardless of round count.

      ## Decision logic

      Actionable items are:
      - CI failures (always)
      - Bot review blockers (only if `auto_fix_rounds < 3`)
      - Rebase needed (always)
      Note: `@bot` requests do NOT come through check — they arrive via poll → fix directly.

      If any actionable items exist → transition to `fix` with the list of issues.
      If no actionable items → transition to `poll`.

      ## Round limit reached (`auto_fix_rounds >= 3`)

      When the round limit is reached and there are still unresolved bot review blockers:
      1. Post a comment on the MR/PR summarizing the remaining unresolved bot review issues
         (both blocker and major), prefixed with `[from bot]`. Example:
         ```
         [from bot] Auto-fix round limit (3/3) reached. Remaining unresolved review issues:
         - [BLOCKER] <file>:<line> — <summary of issue>
         - [MAJOR] <file>:<line> — <summary of issue>
         These require manual attention or can be addressed via `@bot` requests.
         ```
      2. If no other actionable items (no CI failures, no rebase needed) → transition to `poll`
         (idle mode). The workflow continues monitoring for `@bot` mentions, merge, and
         close events but will not auto-fix bot review issues.
    todos:
      - Check pipeline for failed jobs
      - Read current auto_fix_rounds counter from /tmp/mr_lifecycle_state.json
      - Check for unresolved bot inline review comments — parse severity (blocker vs major)
      - Only include blocker-severity comments if auto_fix_rounds < 3
      - Skip major-severity comments entirely (never auto-fix)
      - If round limit reached with remaining blockers, post summary comment on MR
      - Check if rebase is needed (target branch ahead)
    transitions:
      issues found: fix
      all clear: poll

  fix:
    prompt: |
      Fix issues found during check or requested via `@bot`.

      ## Auto-fix round counter

      Track `auto_fix_rounds` via a durable temp file `/tmp/mr_lifecycle_state.json`.
      Read the current value: `cat /tmp/mr_lifecycle_state.json 2>/dev/null | jq '.auto_fix_rounds // 0'`

      **Increment rule**: Increment `auto_fix_rounds` by 1 in this state ONLY when you
      actually fix at least one of the following:
      - CI failures
      - Bot review blockers
      Do NOT increment the counter when fixing `@bot` code change requests — those are
      user-initiated and do not count toward the auto-fix limit.

      After incrementing, write the new value to the temp file:
      `echo '{"auto_fix_rounds": N}' > /tmp/mr_lifecycle_state.json`

      ## Fix sources in priority order (highest first)

      1. **CI failures**: Fetch failed job logs (GitLab API / `gh run view --log-failed`),
         analyze root cause, and fix the code. Pipeline won't pass without these.
      2. **`@bot` code change requests**: If this state was entered via a `@bot` mention
         requesting code changes (the poll state printed the `@bot` context before exiting),
         apply the requested modification. Read the comment body and, for inline threads,
         the file path and line range to understand exactly what the user wants changed.
      3. **Bot review blockers**: Read each unresolved bot-authored review thread with blocker
         severity. Understand the issue described, read the relevant code, and fix it.
         Skip major severity comments — leave for the user to address.

      **User priority rule**: If a `@bot` instruction contradicts a bot review blocker
      (e.g., user says "don't add error handling here" but a bot blocker says "add error
      handling"), follow the user's `@bot` instruction and skip that blocker. The code-review
      pipeline will re-evaluate in its next run.

      **Rebase**: If the target branch is ahead, fetch and rebase. Resolve conflicts autonomously.

      After fixing, run lint/format/unit tests to confirm the fix doesn't introduce new issues.

      If upon inspection there is nothing to fix, transition back to polling.
    todos:
      - Fix CI failures (if any)
      - Fix unresolved bot inline review comments with blocker severity (if any)
      - Handle @bot code change requests (if any)
      - Increment auto_fix_rounds in /tmp/mr_lifecycle_state.json if CI or blocker fixes were applied (not for @bot requests)
      - Rebase (if needed)
      - Run lint/format/unit tests to verify
    transitions:
      fixed: push
      nothing to fix: poll

  push:
    prompt: |
      Commit and push the fixes.

      ## CRITICAL RULE: NEVER resolve threads
      mr-lifecycle MUST NEVER resolve review threads. Do NOT use any thread resolution
      mechanism — no GitLab resolve API (`PUT /discussions/:id` with `resolved: true`),
      no GitHub GraphQL `resolveReviewThread` mutation, no `gh api` calls that resolve
      threads. Thread resolution is exclusively the code-review pipeline's responsibility.
      This applies to ALL threads: bot review threads, `@bot` threads, and any other
      discussion threads.

      ## Actions
      1. **Commit and push** to the remote branch (force push if rebased).
      2. **Comment on addressed review threads**: For each bot review thread that was fixed,
         reply in the thread explaining what was fixed. The reply body MUST start with
         `[from bot]` (e.g., `[from bot] Fixed by adding null check in foo.ts:42`).
      3. **Comment on addressed `@bot` code change threads**: For each `@bot` mention that
         requested a code change and was addressed in this fix cycle, reply in the same
         thread (inline) or as a new comment (issue-level) with body starting with `[from bot]`,
         explaining what was changed. Then add a ✅ reaction to the original `@bot` comment
         as the dedup signal.
      4. **Update the MR/PR description** if the implementation approach changed while
         addressing review feedback or `@bot` requests.
    todos:
      - Commit all changes
      - Push to remote (force push if rebased)
      - Comment on addressed review threads with [from bot] prefix (explain what was fixed)
      - Comment on addressed @bot code change threads with [from bot] prefix
      - React to processed @bot comments with ✅
      - Update MR/PR description if implementation approach changed
      - "NEVER resolve threads — no resolve API calls, no resolveReviewThread mutations"
    transitions:
      pushed: poll

  done:
    prompt: |
      The MR/PR has been merged or closed. Output a summary:
      - MR/PR URL
      - Total auto-fix rounds used (out of 3 max)
      - Total `@bot` interactions handled
      - Brief overview of main fixes applied
    transitions: {}
