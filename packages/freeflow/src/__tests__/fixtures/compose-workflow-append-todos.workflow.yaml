version: 1.2
initial: build
states:
  build:
    workflow: ./child-basic.workflow.yaml
    append_todos:
      - Should not be allowed
    transitions:
      next: done
  done:
    prompt: "Done."
    transitions: {}
