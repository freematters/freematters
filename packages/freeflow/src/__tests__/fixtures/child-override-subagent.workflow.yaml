version: 1.3
initial: start
states:
  start:
    from: "./base-subagent.workflow.yaml#start"
    subagent: false
    transitions:
      next: done
  done:
    prompt: "Child done."
    transitions: {}
