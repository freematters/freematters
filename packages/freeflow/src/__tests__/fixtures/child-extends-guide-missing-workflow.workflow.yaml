version: 1
extends_guide: ./nonexistent-workflow.workflow.yaml
initial: start
states:
  start:
    prompt: "Child start."
    transitions:
      next: done
  done:
    prompt: "Child done."
    transitions: {}
