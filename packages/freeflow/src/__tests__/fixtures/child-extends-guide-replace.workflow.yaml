version: 1.1
extends_guide: ./base-with-guide.workflow.yaml
guide: "Completely new guide."
initial: start
states:
  start:
    prompt: "Child start."
    transitions:
      next: done
  done:
    prompt: "Child done."
    transitions: {}
