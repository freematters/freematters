version: 1
guide: "Guide with literal {{base}} token."
initial: start
states:
  start:
    prompt: "Prompt containing {{base}} placeholder."
    transitions:
      next: done
  done:
    prompt: "Done."
    transitions: {}
