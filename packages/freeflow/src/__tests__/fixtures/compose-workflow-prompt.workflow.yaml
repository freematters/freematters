version: 1.2
initial: start
states:
  start:
    workflow: ./child-simple.workflow.yaml
    prompt: "This should not be allowed."
    transitions:
      completed: done
  done:
    prompt: "Done."
    transitions: {}
